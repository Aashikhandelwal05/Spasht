# Optional: enables live OpenAI-backed reasoning for the Spasht demo.
# Without this key, the app runs with a local fallback so judges can still try the flow.
OPENAI_API_KEY=
SPASHT_OPENAI_MODEL=gpt-5-mini